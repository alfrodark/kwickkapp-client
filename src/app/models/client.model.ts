export interface Client {
  clientId?: string;
  userId?: string;
  name: string;
  email: string;
  phone?: string;
  address?: string;
  street?: string;
  city?: string;
  zip?: string;
  photoUrl?: string;
  photo?: string;
}
